      //eu precio fazer um filtro do historico que retorne somente as criptos que tem a sigla clicada
        //e jogar isso dentro do setHistorico

        /* mas como funciona o .filter() mesmo?
        const resulado = array.filter((entrada, index, array) => argumento)
        resultado = o array que será entregue no final
        array = inicial
        filter = chamada do método
        entrada = o elemento que esta sendo iterado atualmente, ou seja aquilo que o método lerá em cada execução (obrigatório)
        index = índice do elemento citado
        argumento = se trata da função que será executada 
           const array = [ 4, 5, 6, 7, 8 , 10]

        function filtrarParess(elemento, index, array) {
            console.log(`o index atual é ${index} e o array ${array}`);
            return elemento % 2 === 0;
        }

        const resultado = array.filter(filtrarParess)
        console.log(resultado)
            const array = [
            { nome: 'suco de limão',
                preco: 50,
                tipo: 'Suco'
            },
            { nome: 'Breja ',
                preco: 60,
                tipo: 'Cerveja'
            },
            { nome: 'Néctar',
                preco: 90,
                tipo: 'Suco'
            }
        ]

        function filtrarPrecosMenoresQue70(elemento) {
            return elemento.preco < 70;
        }

        const resultado = array.filter(filtrarPrecosMenoresQue70)
        console.log(resultado)


    
        */
